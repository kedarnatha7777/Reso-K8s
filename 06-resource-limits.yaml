Kind: Pod
apiVersion: v1 

metadata: 
  name: resource-limit 

spec:
  containers:
    - name: nginx
      image: nginx
      resources:
        request:
          memory: "64Mi"
          cpu: "100m"
        limit:
          memory: "64Mi"
          cpu: "100m"