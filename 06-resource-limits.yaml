Kind: Pod
apiVersion: v1 

metadata: 
  name: resource-limit 

spec:
  containers:
    - name: nginx
      image: nginx
      resources:
        requests:
          memory: "4Mi"
          cpu: "10m"
        limits:
          memory: "64Mi"
          cpu: "100m"